/*
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */

package model;

import java.io.IOException;

/**
 *
 * @author lenin
 */
public class Menu  extends Principal{
   /*public static String[][] getMenu(String idperfil) throws IOException
    {
         Principal p=new Principal();
        return p.query("vista_modulos where idperfil="+idperfil+" and acceso=0 and submodulo=0 order by idmodulo asc");
    }
     public String[][] getSubMenu(String idperfil,String idmenu) throws IOException
    {
         Principal p=new Principal();
        return p.query("vista_modulos where idperfil="+idperfil+" and submodulo="+idmenu+" and acceso=0 order by idmodulo asc");
    }*/

}
